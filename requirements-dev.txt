# Core testing frameworks
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# Async HTTP testing
aiohttp==3.11.11
aioresponses==0.7.8

# HTML parsing (already in cog requirements, but including for standalone test environment)
beautifulsoup4==4.12.3

# Discord.py for command testing
discord.py==2.5.1

# Optional: Parallel test execution (uncomment if desired)
# pytest-xdist==3.6.1

# Optional: Timeout protection (uncomment if desired)
# pytest-timeout==2.3.1
